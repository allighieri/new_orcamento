---
deployment:
  tasks:
    # Acessa a pasta do reposit��rio no servidor e executa o script de deploy.
    - export DEPLOYPATH=/home/karaoke/repositories/test_production
    - /bin/sh $DEPLOYPATH/deploy.sh